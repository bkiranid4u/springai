info:
  name: openai
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/openai/chat?message=Who are you and What model are you using?
  params:
    - name: message
      value: Who are you and What model are you using?
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
